look at us-states.js and add data

must start up a python server
python -m http.server 8000





** highlight the bar in countyMortRateByStateBarChart
    ** further define function get_current_selection to highlight the specific bar

** Same kind of thing in the scatterplot
